# Быстрый старт: Подключение Supabase к Vercel

## 📋 Чек-лист подключения

### 1️⃣ Получите ключи из Supabase

1. Откройте [Supabase Dashboard](https://app.supabase.com)
2. Выберите проект → **Settings** (⚙️) → **API**
3. Скопируйте:
   - **Project URL** → это `NEXT_PUBLIC_SUPABASE_URL`
   - **anon public** ключ → это `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - **service_role** ключ → это `SUPABASE_SERVICE_ROLE_KEY`

### 2️⃣ Добавьте переменные в Vercel

1. Откройте [Vercel Dashboard](https://vercel.com/dashboard)
2. Выберите проект → **Settings** → **Environment Variables**
3. Добавьте 3 переменные:

   | Key | Value | Откуда взять |
   |-----|-------|--------------|
   | `NEXT_PUBLIC_SUPABASE_URL` | `https://xxx.supabase.co` | Supabase → Settings → API → Project URL |
   | `NEXT_PUBLIC_SUPABASE_ANON_KEY` | `eyJ...` | Supabase → Settings → API → anon public |
   | `SUPABASE_SERVICE_ROLE_KEY` | `eyJ...` | Supabase → Settings → API → service_role |

4. Для каждой переменной выберите все окружения:
   - ✅ Production
   - ✅ Preview  
   - ✅ Development
5. Нажмите **Save**

### 3️⃣ Перезапустите деплой

1. В Vercel Dashboard → **Deployments**
2. Найдите последний деплой → **⋮** (три точки) → **Redeploy**

### 4️⃣ Проверьте подключение

Откройте в браузере:
```
https://catalogcasino.vercel.app/api/health
```

Должен вернуться JSON с `"status": "ok"` или `"status": "warning"` (если таблицы не созданы)

### 5️⃣ Создайте таблицы и добавьте данные

1. [Supabase Dashboard](https://app.supabase.com) → **SQL Editor**
2. Скопируйте содержимое файла `seed-demo-casinos.sql`
3. Вставьте в SQL Editor → **Run**
4. Готово! 🎉

---

## ⚠️ Частые проблемы

### Проблема: "Missing environment variables"

**Решение**: 
- Проверьте, что все 3 переменные добавлены в Vercel
- Убедитесь, что нет лишних пробелов
- Перезапустите деплой после добавления переменных

### Проблема: "Database tables not initialized"

**Решение**: 
- Выполните SQL скрипт `seed-demo-casinos.sql` в Supabase SQL Editor

### Проблема: Переменные добавлены, но не работают

**Решение**:
- Перезапустите деплой (Redeploy)
- Убедитесь, что переменные доступны для нужного окружения (Production/Preview/Development)

---

## 📚 Подробные инструкции

- Полная инструкция: [VERCEL_SETUP.md](./VERCEL_SETUP.md)
- Заполнение данных: [SEEDING.md](./SEEDING.md)
